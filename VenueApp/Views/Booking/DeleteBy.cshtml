@model VenueApp.ViewModels.BookingViewModel

@{
    ViewBag.Title = "Delete Booking from User";
}

<h1>@ViewBag.Title</h1>

<form method="post">

    <div class="col-md-12">
        <h3>
            Are you sure that you want to delete the event:
            from user
            Bookings?
            <a class="back pull-right" asp-controller="Booking" asp-action="Scheduled" asp-route-userId="@Model.User.ID"><i class="fas fa-angle-double-left"></i></a>
        </h3>
    </div>

    <div class="form-group">
        <div class="col-md-12">
            <br>
            <button class="btn btn-lg btn-success" type="submit"><i class="glyphicon glyphicon-ok-sign"></i> YES</button>
            <input type="hidden" name="UserID" value="@Model.UserID" />
            <input type="hidden" name="EventID" value="@Model.EventID" />
            <button class="btn btn-lg" type="button"><i class="glyphicon glyphicon-repeat"></i> NO</button>
        </div>
    </div>>
</form>

<br />
<!--
<p><a asp-controller="User" asp-action="Index">back</a>

-->
